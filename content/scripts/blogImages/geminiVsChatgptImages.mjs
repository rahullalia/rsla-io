/**
 * geminiVsChatgptImages.mjs
 *
 * Generates 3 new inline images for the gemini-vs-chatgpt-vs-claude-for-business post.
 *
 * Usage: node scripts/geminiVsChatgptImages.mjs [--dry-run]
 */

import { createClient } from '@sanity/client';
import { readFileSync, writeFileSync, mkdirSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';
import 'dotenv/config';
import { IMAGE_STYLES, buildPrompt } from './imageStyles.mjs';

const __dirname = dirname(fileURLToPath(import.meta.url));
const OUTPUT_DIR = join(__dirname, '..', 'generated-images');
const DRY_RUN = process.argv.includes('--dry-run');

const GEMINI_API_KEY = process.env.GEMINI_API_KEY;
const SANITY_TOKEN = readFileSync('/Users/rahullalia/lalia/4-Resources/mcp/sanity.env', 'utf-8')
  .split('\n').find(l => l.startsWith('SANITY_API_TOKEN=')).split('=').slice(1).join('=').trim();

if (!GEMINI_API_KEY) throw new Error('Missing GEMINI_API_KEY');
if (!SANITY_TOKEN) throw new Error('Missing SANITY_API_TOKEN');

const DOC_ID = '99d77250-6655-4c78-839e-e429accd51ad';

const sanity = createClient({
  projectId: 'yz25oyux', dataset: 'production', token: SANITY_TOKEN,
  apiVersion: '2025-03-01', useCdn: false,
});

const IMAGES = [
  {
    style: 'whiteboardMarker',
    subject: 'three AI tools with their best use cases',
    details: 'a whiteboard sketch showing three columns drawn with colored dry erase markers: left column in green with a circle and letter G at top (Gemini) with small icons below for email envelope and spreadsheet grid, middle column in blue with a circle at top (ChatGPT) with small icons below for a globe and puzzle piece, right column in purple with a circle at top (Claude) with small icons below for a brain and code brackets, each column has 2-3 use case icons underneath, clean whiteboard background with slight smudge marks',
    alt: 'Whiteboard showing three columns for Gemini (email and spreadsheets), ChatGPT (research and ecosystem), and Claude (reasoning and coding)',
    caption: 'Three tools. Three strengths. Gemini for Workspace. ChatGPT for breadth. Claude for depth. Use all three.',
    afterH2Index: 2, // After "Where each one wins in practice"
  },
  {
    style: 'metricViz',
    subject: 'Claude Code adoption metrics',
    details: 'a hand-drawn bar chart on graph paper with three horizontal bars in blue ink: top bar very long labeled with a percentage icon (4% of GitHub commits), middle bar medium length with a download icon (29M daily installs), bottom bar showing a large dollar amount (2.5B run rate), each bar drawn with slight wobble, clean graphite grid lines, the top bar emphasized with a circle around its end point',
    alt: 'Bar chart showing Claude Code metrics: 4% of GitHub commits, 29 million daily installs, $2.5 billion annualized run rate',
    caption: 'Four percent of all GitHub commits. 29 million daily installs. Those numbers are not marketing claims. That is actual adoption.',
    afterH2Index: 3, // After "The CLI race in 2026"
  },
  {
    style: 'napkinSketch',
    subject: 'monthly cost of the three-tool stack',
    details: 'a napkin sketch in blue ballpoint pen showing three items stacked vertically with prices: top line with a G icon and $14, middle line with a chat bubble icon and $20, bottom line with a brain icon and $20, a horizontal line underneath with a total of $54 circled, below the total a comparison note with a clock icon showing one hour of freelancer time being much more than $54, casual napkin texture background',
    alt: 'Napkin sketch showing the combined cost of all three AI tools: $14 plus $20 plus $20 equals $54 per month total',
    caption: '$54 per month for all three. Less than one hour of freelancer time. The highest ROI investment in the business.',
    afterH2Index: 5, // After "What we actually use at RSL/A"
  },
];

async function generateImage(prompt) {
  const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-image:generateContent?key=${GEMINI_API_KEY}`;
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      contents: [{ parts: [{ text: prompt }] }],
      generationConfig: { responseModalities: ['TEXT', 'IMAGE'] },
    }),
  });
  if (!response.ok) throw new Error(`Gemini ${response.status}: ${await response.text()}`);
  const data = await response.json();
  const img = data.candidates?.[0]?.content?.parts?.find(p => p.inlineData?.mimeType?.startsWith('image/'));
  if (!img) throw new Error('No image from Gemini');
  return { base64: img.inlineData.data, mimeType: img.inlineData.mimeType };
}

async function run() {
  console.log(`\nGemini vs ChatGPT vs Claude: Generate 3 Inline Images (${DRY_RUN ? 'DRY RUN' : 'LIVE'})\n`);

  const doc = await sanity.fetch(`*[_id == $id][0]{body}`, { id: DOC_ID }, { perspective: 'raw' });
  if (!doc?.body) throw new Error('No body');

  const oldImgs = doc.body.filter(b => b._type === 'image').length;
  const textBlocks = doc.body.filter(b => b._type !== 'image');
  console.log(`${doc.body.length} blocks, ${oldImgs} old images removed, ${textBlocks.length} text kept`);

  const h2Indices = [];
  textBlocks.forEach((b, i) => {
    if (b._type === 'block' && b.style === 'h2') {
      h2Indices.push({ idx: i, text: b.children?.map(c => c.text).join('') || '' });
    }
  });
  h2Indices.forEach((h, i) => console.log(`  H2 ${i}: ${h.text}`));

  mkdirSync(OUTPUT_DIR, { recursive: true });
  const newBlocks = [];

  for (let i = 0; i < IMAGES.length; i++) {
    const img = IMAGES[i];
    const prompt = buildPrompt(img.style, img.subject, img.details);
    console.log(`\n  Image ${i + 1}/3: ${img.style}`);

    if (DRY_RUN) { newBlocks.push({ ...img, _type: 'image' }); continue; }

    const { base64 } = await generateImage(prompt);
    const fn = `gemini-chatgpt-claude-inline-${i + 1}.png`;
    writeFileSync(join(OUTPUT_DIR, fn), Buffer.from(base64, 'base64'));
    console.log(`    Saved: ${fn}`);

    const asset = await sanity.assets.upload('image', Buffer.from(base64, 'base64'), { filename: fn, contentType: 'image/png' });
    console.log(`    Uploaded: ${asset._id}`);

    newBlocks.push({
      _type: 'image',
      _key: `img_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`,
      alt: img.alt, caption: img.caption, size: 'full', aspectRatio: '1:1',
      asset: { _type: 'reference', _ref: asset._id },
      afterH2Index: img.afterH2Index,
    });
    if (i < IMAGES.length - 1) await new Promise(r => setTimeout(r, 2000));
  }

  const finalBody = [...textBlocks];
  [...newBlocks].sort((a, b) => b.afterH2Index - a.afterH2Index).forEach(imgBlock => {
    const h2 = h2Indices[imgBlock.afterH2Index];
    if (!h2) return;
    const nextIdx = imgBlock.afterH2Index + 1 < h2Indices.length ? h2Indices[imgBlock.afterH2Index + 1].idx : finalBody.length;
    const { afterH2Index, style, subject, details, ...clean } = imgBlock;
    finalBody.splice(nextIdx, 0, clean);
    console.log(`  Inserted after "${h2.text}"`);
  });

  if (DRY_RUN) { console.log(`Would patch ${finalBody.length} blocks`); return; }

  const res = await fetch('https://yz25oyux.api.sanity.io/v2025-03-01/data/mutate/production', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${SANITY_TOKEN}` },
    body: JSON.stringify({ mutations: [{ patch: { id: DOC_ID, set: { body: finalBody } } }] }),
  });
  const result = await res.json();
  console.log(`\nTransaction: ${result.transactionId}`);

  const v = await sanity.fetch(`*[_id == $id][0]{"t":count(body),"i":count(body[_type=="image"]),"b":count(body[_type=="block"])}`, { id: DOC_ID }, { perspective: 'raw' });
  console.log(`DONE: ${v.t} blocks (${v.i} images, ${v.b} text)\n`);
}

run().catch(e => { console.error(e.message); process.exit(1); });
